@using Bakdelar.Classes;
@model List<ShoppingBasketItem>

@{
    decimal totalPrice = 0;
    if (Model != null && Model.Any())
    {

        totalPrice = Model.Sum(item => item.Price * item.ItemCount);
    }
}
Totalt: @totalPrice kr
